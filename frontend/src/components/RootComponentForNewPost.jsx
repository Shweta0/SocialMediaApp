import { Box } from "@mui/material";
import { styled } from "@mui/system";

const RootComponentForNewPost = styled(Box)(({ theme }) => ({
  borderRadius: "0.75rem",
}));

export default RootComponentForNewPost;
