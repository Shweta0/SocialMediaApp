import { Box } from "@mui/material";
import { styled } from "@mui/system";

const RootComponentForProfile = styled(Box)(({ theme }) => ({
  backgroundColor:'#e0ebeb',
  borderRadius: "0.75rem",
}));

export default RootComponentForProfile;
