import { Box } from "@mui/material";
import { styled } from "@mui/system";

const RootComponentForPostAdded = styled(Box)(({ theme }) => ({
  borderRadius: "0.75rem",
  display:'flex',
}));

export default RootComponentForPostAdded;
