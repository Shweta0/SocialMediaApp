import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexBelow = styled(Box)({
  display: "flex",
  flexDirection:"column",
  alignItems: "center",
});

export default FlexBelow;
